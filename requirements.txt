# >=0.10: the PyTorch cu128 index (below) only ships a stale vllm 0.9.0.1,
# which lacks sm_120 kernels and breaks on Blackwell (RTX 50-series) GPUs.
vllm>=0.10
numpy
pyyaml
httpx
wandb
pygame 
Pillow
openai
pandas
matplotlib
python-dotenv

prometheus-fastapi-instrumentator==7.1.0
fastapi==0.136.1
starlette==0.52.1
uvicorn==0.49.0

# cu128 builds for torch/torchvision. uv prioritizes this extra index and, with
# its default first-index strategy, would lock *every* package (incl. vllm) to it.
# uv.toml sets index-strategy = "unsafe-best-match" so each package takes its best
# version across both indexes (cu128 for torch, PyPI for the modern vllm).
--extra-index-url https://download.pytorch.org/whl/cu128

torch
torchvision
